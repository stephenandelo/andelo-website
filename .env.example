# Staging basic auth (set these in Vercel → Project → Settings → Environment Variables)
# Scope: Preview only (or specifically available when deploying the `staging` branch).
# Never set these on Production.
STAGING_USER=
STAGING_PASSWORD=
